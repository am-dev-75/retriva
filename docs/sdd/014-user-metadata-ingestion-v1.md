# SDD Pack — User-Provided Metadata (v1)

This pack adds support for **user-provided metadata** to `ingestion_api_v1`.

Metadata is supplied by ingestion clients (e.g. thin adapters parsing Open WebUI
chat directives) and persisted by Retriva OSS without interpretation.

The feature is backward-compatible and additive.
