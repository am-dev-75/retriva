# SDD Pack — 013 Markdown Injector

This pack adds a **new injector** for indexing local Markdown files.

The pack is designed so a Markdown corpus root can be explored recursively from the CLI and extracted into section-aware, citation-friendly chunks.
