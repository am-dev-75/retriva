# Acceptance Criteria — Markdown Injector

- A user can point the CLI at a root directory containing local Markdown files.
- The root directory is explored recursively.
- Markdown documents are converted into indexed documents/chunks.
- Title and section-aware metadata is preserved where practical.
- Existing ingestion modes still work unchanged.
- The repository root README.md remains untouched.
