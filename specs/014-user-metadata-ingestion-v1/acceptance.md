# Acceptance Criteria — User-Provided Metadata (v1)

- Ingestion requests may include `user_metadata`
- Documents store provided metadata
- All chunks inherit metadata
- Retrieval filters can use metadata
- Existing ingestion flows are unaffected
